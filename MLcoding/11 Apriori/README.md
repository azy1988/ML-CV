1.关联分析的目标包括两项：发现频繁项集和发现关联规则。所谓频繁项集就是经常出现在一起的特征集合；关联规则指的是频繁项集中的某个元素或者某个元素集合可能会推导出另一个元素。

2、Apriori原理：如果某个项集是频繁项集，则其所有子集都是频繁集；如果某个项集不是频繁项集，则其所有超集都不是频繁项集。
具体操作时，循环进行如下步骤：（1）由数据集生成候选项集C1；通过支持度过滤，生成频繁项集L1；（2）对L1的数据生成候选项集C2（两两拼合实现），通过支持度过滤，生成频繁项集L2。循环知道频繁项集仅有一个为止。

